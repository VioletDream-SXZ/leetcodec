#ifndef __VECTOR_UTILS_H__
#define __VECTOR_UTILS_H__

#include <vector>
#include <string>

namespace Utils {
    void display_int_vector(std::vector<int>& vc);

    void display_int_vector(std::vector<std::string>& vc);
}

#endif // __VECTOR_UTILS_H__